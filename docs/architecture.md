# Архитектура

## Стек

| Слой | Технология | Зачем |
|---|---|---|
| Backend | Python 3 + Flask 3 (SSR, Jinja2) | Интерфейс рендерится на сервере — максимально похож на pikabu.ru без сборки и SPA |
| БД | SQLite (один файл `data/app.db`) | Ноль настройки, данные локально, достаточно для одной библиотеки |
| Парсинг EPUB | `ebooklib` + `lxml` | Разбор spine/TOC/манифеста и безопасная чистка HTML |
| Парсинг FB2 | `xml.etree.ElementTree` (stdlib) | FB2 — простой XML, зависимость не нужна |
| Фронтенд | Чистые HTML/CSS/JS без фреймворков | `static/js/reader.js` (~250 строк) — рейтинги, заметки, прогресс, настройки |

Запуск: `python app.py` → http://127.0.0.1:8000 (порт задан в `app.py`).

## Структура проекта

```
pikabureader/
├── app.py               # Flask-приложение: роуты, JSON API, импорт книг
├── pika.py              # CLI: serve / import / list / stats / backup (--json)
├── db.py                # схема SQLite, подключение, настройки
├── parsers/
│   └── __init__.py      # ParsedBook, парсеры EPUB/FB2, санитайзер HTML
├── templates/           # Jinja-шаблоны
│   ├── base.html        # шапка, сайдбары, подключение стилей/скриптов
│   ├── feed.html        # лента глав (карточки-посты), пагинация
│   ├── book.html        # страница книги (оглавление, прогресс)
│   ├── story.html       # страница главы + блок заметок
│   ├── library.html     # сетка обложек (Библиотека)
│   ├── list.html        # список глав (Закладки)
│   └── add.html         # импорт с drag&drop
├── static/
│   ├── css/pikabu.css   # клон интерфейса Pikabu, светлая/тёмная тема
│   └── js/reader.js     # поведение UI (см. «Клиентская логика»)
├── data/                # создаётся автоматически
│   ├── app.db           # SQLite
│   ├── uploads/         # загруженные файлы книг
│   ├── covers/          # обложки (book_<id>.jpg)
│   └── images/book_<N>/ # картинки глав
└── requirements.txt     # Flask, ebooklib, lxml
```

## Схема БД

```
books(id, title, author, cover, tags, description, created_at)
  └─ chapters(id, book_id→books, ord, title, html, words, source, rating, created_at)
       ├─ notes(id, chapter_id→chapters, parent_id→notes, text, rating, created_at)
       └─ state(chapter_id→chapters PK, bookmark, done, read_pct, last_read_at)
settings(key PK, value)   -- theme, font, size, width
```

- `chapters.html` — очищенный HTML главы, в ссылках на картинки подставлен путь `/media/images/<book_id>/...`.
- `chapters.source` — файл-источник главы в архиве EPUB (для ссылок `/goto`); пусто у FB2/PDF/TXT.
- `notes.parent_id` — дерево заметок (ответы); каскадное удаление при удалении книги/главы.
- `state` — одна строка на главу: закладка, флаг «прочитано», прогресс в процентах. При обновлении прогресса применяется `MAX` — позиция не откатывается при повторном открытии сверху.
- `settings` — глобальные настройки чтения, читаются в context processor и попадают в `window.READER.settings`.

Все подключения включают `PRAGMA foreign_keys = ON`.

## Маршруты

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/` | Лента. Параметры: `t=hot\|new\|library\|reading`, `p` (страница), `q` (поиск по книге/автору) |
| GET | `/bookmarks` | Список закладок |
| GET | `/history` | «Недавно читал» — последние 20 глав профиля (N7) |
| GET | `/book/<id>` | Страница книги с оглавлением |
| GET | `/story/<id>` | Страница главы + заметки |
| GET | `/goto/<book_id>/<path>` | Внутрикнижная ссылка (сноска): редирект на главу с якорем `?frag=`, файл не импортирован — на первую главу книги |
| GET/POST | `/add` | Форма импорта; POST с `file` парсит книгу и создаёт записи |
| GET | `/covers/<name>` | Отдача обложек |
| GET | `/media/images/<book_id>/<name>` | Отдача картинок глав |

## JSON API

Все запросы — POST с JSON, ответ `{"ok": true, ...}`; ошибки — 400 с `{"ok": false, "error"}`.

| Путь | Тело | Что делает |
|---|---|---|
| `/api/rate` | `{target: "chapter"\|"note", id, delta: 1\|-1}` | Оценка главы или заметки |
| `/api/chapter` | `{chapter_id}` | Полный HTML главы — «Читать далее» разворачивает карточку в ленте (R6) |
| `/api/progress` | `{chapter_id, pct, done?}` | Позиция чтения (`read_pct = MAX(...)`) |
| `/api/bookmark` | `{chapter_id}` | Переключает закладку, возвращает новое состояние |
| `/api/note` | `{chapter_id, parent_id?, text}` | Создаёт заметку/ответ, возвращает запись |
| `/api/note/delete` | `{id}` | Удаляет заметку (с потомками — каскад) |
| `/api/book/delete` | `{book_id}` | Удаляет книгу + файлы обложки и картинок |
| `/api/settings` GET/POST | `{theme, font, size, width}` | Чтение/сохранение настроек чтения |

## Парсеры

Оба парсера возвращают `ParsedBook`: `title, author, tags, description, cover(bytes), chapters[(title, html)], assets{name: bytes}, sources[файл-источник главы]` (заполняется у EPUB). Импорт (`app.py`): создаёт книгу, сохраняет обложку/картинки, переписывает внутрикнижные ссылки на `/goto`, вставляет главы, подставляет пути `/media/images/`.

### Общая чистка HTML (`_clean_fragment`)

1. Удаляются комментарии, `<?xml?>`, DOCTYPE.
2. Проход 1 (по снимку дерева — модификация во время `.iter()` запрещена, это был баг): вырезаются `script/style/head/meta/title`.
3. Проход 2: неизвестные теги «разворачиваются» в детей (но если у тега есть `id` — он превращается в `<span id>`, чтобы не потерять якорь сноски), атрибуты остаются только `img[src,alt]`, `a[href,title,id]`, у остальных — только `id`.

### EPUB

1. `ebooklib.read_epub` → метаданные DC (title/creator/subject/description), обложка.
2. Карта картинок: путь в архиве → item; ссылки `src` разрешаются относительно документа (`posixpath` + `unquote`).
3. Главы идут в порядке `spine`; названия — из TOC, иначе из `h1–h3`, иначе «Глава N».
4. **Страница без текста**: обложка (`cover` в src/alt) пропускается; иллюстрация запоминается и приклеивается в начало следующей текстовой главы.
5. Если глава всего одна — разрезается по абзацам (~3500 знаков) через `_split_html`.

### FB2

1. **Кодировка**: utf-8 → windows-1251 → с заменой битых символов.
2. **Неймспейс**: определяется из корневого тега (поддерживает `fb2.1` и `fictionbook/2.0` — на этом ломались реальные книги).
3. **Картинки**: `<binary id="...">base64</binary>` из файла + внешние файлы рядом/в zip.
4. **Body**: берётся body без имени `notes/comments/footnotes` — сноски не превращаются в главы.
5. **Разбивка секций** (`render_section` → `flatten` → `collect`):
   - секция без вложенных = готовая глава;
   - вложенные секции, если общая длина ≤ **80 000 знаков**, остаются внутри главы (заголовки вложенных → `h3/h4`);
   - крупнее — каждая подсекция становится главой, а в заголовок добавляется имя обёртки: `«Часть I. «Три пути» — Глава 1. Agile...»`;
   - содержимое обёртки меньше **500 знаков** (эпиграф, декоративная картинка) приклеивается к первой подсекции, а не создаёт пустую главу.
6. Заголовки/цитаты/стихи/подзаголовки секции рендерятся с сохранением `strong/em/sup/sub`.

## Клиентская логика (`static/js/reader.js`)

- **Настройки чтения**: применяет `theme/font/size/width` к `[data-theme]` и `#reader`, шлёт `POST /api/settings`; переключатель темы — в шапке.
- **Рейтинги/закладки/удаление книги** — делегирование кликов, optimistic-обновление DOM.
- **Прогресс**: `scroll` + debounce 400 мс → `pct` от позиции `#reader`; при ≥95% — `done`; отправка `beforeunload`.
- **Заметки**: отправка формы (с учётом `replyTo`), вставка узла ответа в `.note-children` без перезагрузки, удаление с переносом дочерних узлов.
- **Поделиться** — копирование ссылки в буфер обмена.

## Тестирование

- `python -m pyflakes app.py db.py parsers tests` — линт (в CI, job `test`).
- `python -m pytest tests/ -q` — pytest-набор (в CI): парсеры, миграции БД, роуты и JSON API, стили.
- E2E smoke (Q4): `tests/test_e2e.py` — Playwright со системным Chrome (headless) против живого сервера на эфемерном порту с временной базой: импорт EPUB → лента → страница главы → заметка. Без установленного `playwright` тест скипается.
- Функциональная проверка: импорт тестовых EPUB/FB2, обход всех маршрутов (200), вызовы JSON API, отдача обложек/картинок.
- Ограничение: скриншотных тестов нет — вёрстка проверяется вручную в браузере.
